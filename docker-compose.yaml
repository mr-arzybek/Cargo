version: '3'

services:
  backend:
    build: .
    container_name: backend
    ports:
      - "80:80"
    environment:
      - DEBUG=True
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:80"  # Команда для миграции и запуска сервера